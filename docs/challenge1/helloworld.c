#include <stdio.h>

struct node{
    struct node* previous;
    char* value;
    struct node* next;
};

int main(void){
    char* string = malloc(50);
    string = "hello world";
    struct node* MasterNode = malloc(sizeof(struct node));
    MasterNode
    printf("hello world");
    return 8008135;
}